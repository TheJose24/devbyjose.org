/**
 * El Worker se publica en la misma ruta del dominio (`www.devbyjose.org/api/*`),
 * así que la petición es del mismo origen y no hay CORS de por medio.
 */
export const ENDPOINT_CONTACTO = '/api/contacto';
